# ML-HR 해야 할 일 체크리스트

## 1. 문서 목적

이 문서는 현재 구현 현황을 바탕으로  
팀이 다음으로 진행해야 할 작업을 체크리스트 형태로 정리한 문서입니다.

우선순위 기준은 다음과 같습니다.

- 먼저 고치지 않으면 이후 작업이 꼬이는 항목
- 평가 파이프라인의 핵심 기능을 만드는 항목
- 발표 및 시연에 직접 필요한 항목

## 2. 최우선 정리 항목

### 진행 반영 메모
- 아래 체크 상태는 `2026-03-16` 기준 현재 저장소 반영 상태입니다.

### 파이프라인 기준 통일
- [x] 메인 파이프라인을 `collect.py + evaluate.py` 기준으로 갈지 확정하기
- [x] `auto_pipeline.py`를 유지할지, 데모용으로 분리할지 결정하기
- [x] 실행 진입점을 하나로 통일하기
- [x] `run_all.bat` 외에 표준 실행 방식 문서화하기

### DB 스키마 정리
- [x] `news_data` 테이블 컬럼 목록 확정하기
- [x] `summary` 컬럼과 `summary_kobart`, `summary_kot5`, `summary_roberta`, `summary_mistral` 구조 중 어떤 방식으로 갈지 결정하기
- [ ] 뉴스 원문, 전처리 결과, 모델별 결과, 평가 점수를 어떤 컬럼에 저장할지 정리하기
- [ ] 중복 뉴스 판별 기준 정의하기

### 실행 환경 정리
- [x] `requirements.txt` 작성하기
- [ ] 필수 라이브러리 버전 고정하기
- [ ] `.env`에 필요한 환경변수 목록 정리하기
- [x] README 실행 방법을 실제 코드 기준으로 수정하기

## 3. 데이터 수집/전처리 개선

### 뉴스 수집 안정화
- [ ] 뉴스 중복 저장 방지 로직 추가하기
- [ ] API 요청 실패 시 재시도 또는 예외 처리 추가하기
- [ ] 본문 크롤링 실패 원인 로그 남기기
- [ ] `description` 대체 사용 시 HTML 정리 여부 확인하기

### ETL / 전처리 보강
- [ ] `수집 -> 정제 -> 저장` 흐름으로 단계 분리하기
- [ ] 전처리 결과를 재사용할지 즉시 처리할지 결정하기
- [ ] 기사 길이 기준 필터링 규칙 정하기
- [ ] 카테고리별 데이터 수집 정책 정하기

## 4. 모델 추론 파이프라인 개선

### 모델 로딩 구조 개선
- [ ] 기사마다 모델을 다시 불러오는 구조 수정하기
- [ ] 모델은 1회 로드 후 여러 기사에 재사용되도록 변경하기
- [ ] GPU/CPU 사용 기준 정리하기
- [ ] 메모리 부족 시 대응 방식 정하기

### 모델 역할 정리
- [ ] `KoBART`, `KoT5`, `Mistral-7B`를 생성형 모델로 정의하기
- [ ] `KLUE-RoBERTa`를 추출형 기준 모델로 명시하기
- [ ] `GPT-3.5`를 실제 구현 범위에 포함할지 결정하기

## 5. 평가 및 인사고과 구현

### 평가 지표 추가
- [ ] `ROUGE` 계산 로직 구현하기
- [ ] `Latency` 측정 로직 구현하기
- [ ] `Cost` 계산 기준 정의하기
- [ ] `Stability` 또는 실패율 측정 여부 결정하기

### 기준 답안 정리
- [ ] 평가용 정답 텍스트를 무엇으로 둘지 정하기
- [ ] 제목을 정답으로 쓸지, 본문 핵심문장을 만들지 결정하기
- [ ] 자동 평가가 어려운 경우 수동 평가 기준 마련하기

### 인사고과 로직 구현
- [ ] 모델별 점수 집계 방식 정하기
- [ ] 가중치 기반 최종 점수 계산식 정하기
- [ ] 최우수 모델 선정 기준 확정하기

## 6. MLflow 및 실험 관리

### MLflow 연동
- [ ] `mlflow`를 실제 코드에 연동하기
- [ ] 모델별 실험 run 생성하기
- [ ] 평가 지표를 MLflow에 기록하기
- [ ] 생성된 요약 결과를 artifact로 저장할지 결정하기

### 실험 관리 구조
- [ ] 모델명, 데이터 버전, 실행 시간 등을 기록할 항목 정의하기
- [ ] 실험 재현을 위한 실행 조건 저장 방식 정하기
- [ ] 최우수 모델 결과를 따로 관리할지 결정하기

## 7. 배포 준비

### API 서빙
- [ ] `FastAPI` 기본 서버 만들기
- [ ] 선택된 모델을 호출하는 요약 API 만들기
- [ ] 입력값/출력값 형식 정의하기
- [ ] 추론 실패 시 예외 응답 규칙 정하기

### 컨테이너 및 배포
- [ ] `Dockerfile` 작성하기
- [ ] 로컬에서 컨테이너 실행 확인하기
- [ ] Render 배포용 설정 정리하기
- [ ] 환경변수 주입 방식 정리하기

## 8. 모니터링 및 운영

### 품질 모니터링
- [ ] `Evidently AI` 적용 범위 정하기
- [ ] 데이터 드리프트 기준 정의하기
- [ ] 품질 저하 판단 기준 정하기

### 시스템 모니터링
- [ ] `Prometheus` 수집 대상 정하기
- [ ] `Grafana` 대시보드 항목 정하기
- [ ] 응답 속도, 에러율, 처리 건수 지표 정의하기

### 재학습 흐름
- [ ] 성능 저하 시 재실험 조건 정의하기
- [ ] 수동 재학습으로 갈지 자동 재학습으로 갈지 결정하기
- [ ] 재학습 후 모델 교체 절차 정리하기

## 9. 발표 및 협업 준비

### 문서 정리
- [x] README를 현재 구현 상태에 맞게 업데이트하기
- [ ] 아키텍처 설명 문구 최종 정리하기
- [ ] 구현 현황 문서와 체크리스트 최신 상태 유지하기

### 역할 분담
- [ ] 데이터 수집 담당 정하기
- [ ] 모델 평가 담당 정하기
- [ ] 배포 담당 정하기
- [ ] 발표 자료 담당 정하기

## 10. 추천 작업 순서

### 1단계
- [x] 파이프라인 기준 통일
- [x] DB 스키마 정리
- [x] requirements.txt 작성

### 2단계
- [ ] 모델 로딩 구조 개선
- [ ] 평가 지표 추가
- [ ] MLflow 연동

### 3단계
- [ ] 최우수 모델 선정 로직 구현
- [ ] FastAPI 서버 구현
- [ ] Docker 적용

### 4단계
- [ ] 모니터링 추가
- [ ] 발표 자료 정리
- [ ] 시연 시나리오 준비
